# R10.0 — Full React Production Cutover

Primary production UI is the Next.js/React static export. Legacy Vanilla UI remains only as local emergency rollback source and is not copied under `wwwroot`.

Added: Restore Explorer with Agent command polling, granular restore, Pilot Readiness / Deep Validation, Management API tokens, break-glass recovery codes, and Notification Delivery history.

Preserved: exact deployment binding, R6.7 Mesh heartbeat-aware reconciliation, R6.8 repository AutoKey, R6.9 Global NAS, R7.0 encrypted settings transfer, repository format, Agent protocol and .NET 10 backend behavior.
